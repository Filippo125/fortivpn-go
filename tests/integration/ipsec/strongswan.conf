charon {
    load_modular = yes
    plugins {
        include /etc/strongswan.d/charon/*.conf
        resolve { load = no }
        updown { load = no }
    }
    filelog {
        stderr {
            default = 1
            ike_name = yes
            flush_line = yes
        }
    }
}
